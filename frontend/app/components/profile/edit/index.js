import Component from '@glimmer/component';
import { inject as service } from '@ember/service';

export default class ProfileEditIndexComponent extends Component {

      @service session;
      @service fastboot;

}
